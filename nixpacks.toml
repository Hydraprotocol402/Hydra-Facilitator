[phases.setup]
nixPkgs = ['nodejs_22', 'pnpm', 'openssl']

[phases.install]
cmds = ['pnpm install --frozen-lockfile']

[phases.build]
cmds = ['pnpm run build', 'cd examples/nestjs && pnpm run build']

[start]
cmd = "cd examples/nestjs && pnpm run start:prod"
