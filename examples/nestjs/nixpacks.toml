[phases.setup]
nixPkgs = ['nodejs_20', 'npm-9_x', 'openssl']

[phases.install]
cmds = ['npm install -g pnpm', 'cd ../.. && npx pnpm install']

[phases.build]
cmds = ['cd ../.. && npx pnpm build && cd examples/nestjs && npx pnpm build']

[start]
cmd = "node dist/main.js"
